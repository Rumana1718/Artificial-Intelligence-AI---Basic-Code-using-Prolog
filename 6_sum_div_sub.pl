summation(sum, sum, sum). 
subtraction(sub,sub,sub).
division(div,div,div).
multiplication(product, product, product) .
summation(X, Y, Sum) :- Sum = X + Y.
subtraction(X,Y,Sub) :- Sub = X - Y.
 division(X,Y,Div) :- div = X / Y.
multiplication(X, Y, Product) :- Product = X * Y.
